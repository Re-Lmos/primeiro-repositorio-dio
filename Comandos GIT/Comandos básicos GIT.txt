git config --global user.name 'Renata'
git config --global user.email 'email'
 //configurar 

git init
 //

git status
 //vai mostrar status 

git add . 
 //envia commit

git clone 'chave ssh'
 //vai clonar o repositório remoto para o local do Git

git push origin main
  //manda para o repositório remoto
